# 用户实机内饰修改存档 · 2026-09-05

用户确认第一版搬迁视觉后，在「燕隼号·搬迁验收」中手工修改了内饰。本目录保存该版本。
随后根据用户要求，此处的 NBT 已逐字节复制为内置 `starter_ship.nbt`，用作默认初始飞船。
之后又按要求仅在内置模板的储物柜中加入 1 把求生匕首和 3 罐罐头；此处归档仍保留未加补给的原始版本。
`manifest.json` 保留导出时的历史状态；当前接入状态见 [实施说明](../../shuttle-ship-import-plan.md)。

读取前已确认 Minecraft 退出，日志显示 2026-09-05 20:10:55 所有维度保存完成。
来源为 `run/saves/shuttle-import-review`，飞船维度 `starboundmc:ship`。

## 保存内容

- `shuttle-interior.nbt`：Minecraft 1.21.1 原版结构模板，1,178 个非空气方块、7,171 个显式空气格、3 个方块实体。
- `blocks.json`：世界坐标逐格清单，包含全部方块状态和方块实体 SNBT，便于对比与后续接入。
- `source-save.zip`（仅本地，不提交）：完整原始存档备份，保留玩家背包、进度、其余维度、飞船容器及其他存档数据；省略运行时锁文件。
- `manifest.json`（仅本地，不提交）：来源、材料统计、坐标范围、源文件和导出文件 SHA-256，以及验证结果。

导出结构包含空气，因此用户拆除的隔墙或设备也能被保留下来。容器数据按原样保存，没有清空物品或改动机器状态。
本次源存档没有实体；结构文件中的实体列表为空。

## 放置坐标

尺寸按 X/Y/Z 为 **23×11×33**。世界范围为 `(-11,100,-15)` 至 `(11,110,17)`，船头朝 +Z。

模板内坐标从 `(0,0,0)` 起算，正确放置角点为 **`(-11,100,-15)`**。
不要直接将此文件重命名为默认 `ship.nbt`：现有可选模板入口使用 `(0,102,0)`，直接替换会导致整体错位。
默认生成器使用独立的 `starter_ship.nbt` 及正确原点，设备共享坐标已同步到本存档布局。

## 验证

已回读 NBT，逐格验证方块与全部状态，并还原坐标比较所有方块实体 NBT；全部与源存档一致。
完整备份通过 ZIP CRC 与逐文件 SHA-256 检查；读取前后源存档文件哈希一致，原存档未改动。

重新导出其他版本时，关闭世界后运行 `scripts/export-ship-save.py`，输出到新的目录。脚本拒绝覆盖已有导出。
