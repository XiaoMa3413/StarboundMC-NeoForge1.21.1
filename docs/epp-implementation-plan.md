# EPP 环境生存实施计划

基线：`codex/epp-mechanisms` / `e11661e`。2026-09-18。

依据《StarboundMC EPP、个人机动装备与环境生存系统设计》及本次对话确认。
本文件记录实施决策和实际验证；设计文档中与下列确认冲突的建议不作为施工要求。

## 已确认的范围（R0–R5 历史基线；后续实施见 R6、R7a、R7b）

- 本轮执行 R0–R5：Mk.I、独立装备槽、有限氧气、补给设备、氧气罐、月球补给与引导。
- Mk.I 在 Lush 即可制作，不要求 Barren 专属核心。新增通用基础电路板（Circuit Tier 0），
  用于 EPP、补给设备及其他基础科技。Barren 的高级元件、升级奖励留给后续阶段。
- Frozen/Molten 可呼吸，冷热与供氧独立。本轮不启用尚无防护装备可应对的冷热伤害。
- 普通可呼吸环境停止耗氧；默认飞船明确划定的舱内区域自动补氧。首版不检测拆墙失压。
- 玩家自行制作并摆放补给设备；不修改默认飞船模板。设备在有可呼吸空气处提供灌装。
- 独立 EPP 槽，不占胸甲；唯一 Active EPP，第三方槽位预留接口，无强制饰品依赖。
- 氧气随物品保存。允许携带和更换备用 EPP；模块安装限制留到 Mk.II。
- 死亡遵循 keepInventory，重登/换装不重置窒息缓冲。水下呼吸维持原版行为。
- 初始调参：Mk.I 720 单位，正常无氧环境每秒 1 单位；氧气罐 180 单位，使用 2 秒，
  受伤中断，只在能完整容纳时使用；满罐不堆叠，空罐叠 8；耗尽缓冲 10 秒。
- 不提交、不推送；用户原有未跟踪文档与 output/tmp 不纳入本轮整理。

## 分阶段执行

| 阶段 | 工作 | 状态 / 实际验证 |
| --- | --- | --- |
| R0 | 确认决策，盘点基线，记录计划 | 已完成。开始时无已跟踪文件修改；LDLib2 基线脚本通过：2.2.36.a |
| R1 | 统一环境查询、区域覆盖、星球大气纠正 | 已实现；2 项环境单测通过；GameTest 验证区域供氧差异；实际维度旅行待验收 |
| R2 | 原生槽位、唯一 Resolver、Mk.I、存档及死亡行为 | 已实现；GameTest 验证装备转移、多人独立、来源优先级、死亡掉落、存档读写 |
| R3 | 氧气、窒息、HUD、NOVA 警告 | 已实现；8 项氧气规则单测通过，真实物品耗氧测试通过；HUD/声音待实机 |
| R4 | 氧气罐、补给设备、菜单与制造配方 | 已实现；GameTest 验证整罐容量、空罐返还、灌装进度读写、多人取罐不复制 |
| R5 | 通用基础电路板、月球补给、任务引导、基础美术、回归 | 已实现；任务迁移及资源回归通过；完整 build 成功；生存节奏与视觉待人工验收 |
| R6 | 模块、维护工作站、Mk.II 单槽平台、寒冷防护闭环 | 已实现并提交推送 `9b4dec0`；552 项单元测试、24 项 GameTest 及 build 通过；EVA 后续见 R7a |
| R7a | Mk.II EVA、船外零重力、返船信标、船员跃迁安全 | 已实现；562 项单元测试、30 项 GameTest 及 build 通过；含误出舱紧急召回，实机操控与真实多人待验收 |
| R7b | 首个废弃中继站、动态安全锚点、接近代理、探索奖励与持久化 | 已实现；569 项单元测试、34 项 GameTest 及 build 通过；真实多人、视觉与旧存档重启待人工验收 |

每阶段补充实际验证，不将编译或单元测试等同于实机验收。

## 实施参数与配方初值

以下为可调实现初值，不是永久平衡结论。复用现有打印配方及个人 Voxel 钱包。

| 产物 | 材料 | Voxels | 打印时间 |
| --- | --- | --- | --- |
| 基础电路板 ×2 | 铜锭 ×2、红石 ×2、铁锭 ×1 | 10 | 2 秒 |
| EPP Mk.I | 基础电路板 ×2、铁锭 ×8、铜锭 ×4、玻璃 ×2 | 50 | 5 秒 |
| 生命维持补给站 | 基础电路板 ×2、铁锭 ×6、铜锭 ×2、玻璃 ×2 | 50 | 5 秒 |
| 空氧气罐 ×1 | 铁锭 ×2、玻璃 ×1 | 10 | 2 秒 |
| 工业灯 ×4（新增打印途径） | 基础电路板 ×1、铁锭 ×2、玻璃 ×2 | 10 | 2 秒 |

基础电路板仅使用 Lush 可获得的普通材料。新制造的 Mk.I 氧气为零，装备后通过飞船舱内或补给站充氧。
补给站一次灌装一只空罐，1 秒；在可呼吸环境工作，无能源消耗。
EPP 在受控供氧区每秒恢复 240 单位，满罐储量与初始容量统一配置。

## 验证矩阵

- 环境：月球无氧、Frozen/Molten 可呼吸、非目标模组维度不凭空增加危险；按玩家位置查询。
- 生命周期：换装、丢弃、交易、死亡两种 keepInventory、重连、正常重启、旧存档默认值。
- 服务端：多人分别耗氧；重复补给操作不复制物品；满槽、关闭菜单、断线和拆除设备安全。
- 氧气：无 EPP、耗尽缓冲、缓冲连续性、恢复呼吸、满容量限制、临界容量用罐、受伤中断。
- 客户端：中文/英文、GUI 缩放、装备/背包槽拖动、HUD 阈值和 NOVA 提醒不刷屏。
- 生存：Lush 材料到 Mk.I；飞船补氧；携带补给探索月球；只在指定前哨提供稀疏补给。

## 后续阶段

R6：用户已于 2026-09-18 授权继续实施模块、工作站和首个环境危险闭环，见下节。
R7a：先完成 EVA 出舱/返舱基础，见末尾交付记录。
R7b：首个废弃中继站闭环已实现，见末尾交付记录；完整实机验收仍待完成。
R8：Mk.III/IV、后续防护、Mobility Rig/Jump Thruster；Barren 高级科技与升级奖励。
不实现气体模拟、动态密闭、能量系统、随意扩张的模块数量或对危险星球的装备硬门禁。

### R6 本轮实施范围（2026-09-18，已实现并完成自动验证）

- P0-8～P0-10：模块持久化与防护汇总、EPP 工作站、首个 Heating I 模块、寒冷 Exposure。
- Mk.I 不增加模块槽；先实现 Mk.I → Mk.II 的升级、扩容与一个 Tier I 模块槽。
  Mk.II 的 EVA 内置能力仍属于下一阶段，当前物品与界面不宣称已具备 EVA。
- 升级在工作站消耗升级套件，保留氧气、自定义名称和其他物品组件；安装/拆除不吞模块。
  兼容模块随升级保留；不兼容模块自动返还背包，背包满时按原版规则掉落给玩家。
  工作站在安全、可呼吸环境维护，菜单输入为各玩家独立托盘，关闭时返还，避免多人抢改同一设备。
- 首个危险为寒冷，氧气独立。默认 Frozen 从此前预留的 Cold III 调整为 Cold I，
  使当前可制作的 Heating I 能完整防护；自定义高阶环境仍按防护差额累积暴露。
  默认每秒每阶差额 +2 暴露，安全或足额防护每秒 -5，25/50/75 为预警与症状，100 开始伤害。
- 不启用 Heat/Radiation 伤害，不推进局部太空、EVA 或 Mobility Rig。
  延续已确认的 Mk.I 在 Lush 可制作、向上曲面 HUD 和微增惯性；不提交、不推送。

## 验证记录

- LDLib2 基线检查通过；本地源代码与 2.2.36.a 一致。
- 首次 `compileJava` 通过。
- 首次完整单元测试：528 项，7 项失败。均涉及本次新增注册项、配方数量、网络版本和任务 ID
  扩展后的旧断言；已更新这些兼容边界期望，继续运行专项测试及完整回归。
- 飞船受控舱内范围经模板逐层核对：X -2..2；主舱 Y 101..104、Z -8..9；
  驾驶舱 Y 101..103、Z 10..12。排除船翼和船顶；这是首版明确区域，不是密闭检测。
  区域之外按真空处理，不据此启用 EVA 或改变重力。
- 氧气补给加入现有 Rocky Moon `supply_cache`，每个新生成补给仓固定一罐。
  既有前哨不会被重填，防止刷新已取走的战利品；旧存档可制作氧气罐或探索未生成区域。

- 第二轮完整单元测试通过：541 项，0 失败、0 跳过。
- 首次 GameTest：17 项中 3 项失败，原因是 GameTestServer 只创建原版维度；
  改用环境查询层的区域扩展接口注入真空/受控供氧测试区域。真实月球与飞船维度旅行仍需实机验收。
- 第二次 GameTest：同 3 项在发送快照时失败。嵌入式测试连接没有执行 NeoForge 握手；
  在测试连接中安装协商通道，生产网络校验保持不变。
- 最终 `gradlew.bat -PshipGameTests runGameTestServer --console=plain`：17 项全部通过，
  其中 7 项为新增 EPP 集成测试。日志：`build/epp-gametest.log`。
- 最终 `gradlew.bat build --console=plain` 成功，含 541 项单元测试，0 失败、0 跳过。
  日志：`build/epp-build.log`；保留既有 EventBusSubscriber API 弃用警告。
- 发布 JAR 包含 EPP 类、配方和美术；不包含 GameTest 类。默认飞船 NBT 与基线逐字节一致。
- 中英文本地化键均为 499 个，无重复键；资源引用、配方与 loot table 检查随完整测试通过。
- `git diff --check` 通过；新增源文件与计划文档的尾随空白检查通过。
- 星图详情增加可呼吸/不可呼吸提示，不锁定传送。NOVA 增加“准备生命维持装备”和
  “第一次无氧远征”任务，完成后各可领取一个物质枪模块。

## 游戏内验收入口与剩余项目

1. 默认 **G** 打开个人环境装备页（可在控制设置更改），穿戴 Mk.I；胸甲槽保持可用。
   新制造的设备为空，在默认飞船舱内约 3 秒充满，或在有空气处打开补给站维护。
2. 从打印机制作基础电路板、Mk.I、补给站和空罐。普通 Lush 材料即可完成；
   基础电路板也用于新的工业灯打印配方，旧工业灯配方保留。
3. 将空罐放入补给站槽位，1 秒后取出满罐。离开/关闭界面不取消灌装。
4. **生存模式**前往月球，确认耗氧、低氧提醒、用罐需长按 2 秒且受伤会中断。
   创造/旁观者免除耗氧和窒息，不适合验收消耗速度。
5. 返回飞船补氧，检查 NOVA 两个新增任务、个人奖励及不同玩家的独立进度。
6. 待人工验证：中英 GUI 缩放、拖放与关闭界面、背负模型/动作、HUD 不遮挡、警告声音；
   真实客户端联机、退出重进和完整关服重启、旧存档中实际维度与新前哨补给。

自动测试中的存档验证是实际 Player/BlockEntity NBT 编解码往返，**不是**真实关服重启验收。
没有声称完整客户端联机或视觉测试已通过。Mk.II、模块、EVA、Jump Thruster 仍留在后续阶段。


### 氧气 HUD 与快速补氧调整

- HUD 简化为头盔曲面投影：只保留淡青色 O₂ 百分比与向视野右缘上扬的细弧线，读数沿曲面切线轻微倾斜；无底板、边框或常驻状态说明。补氧时弧线轻微呼吸，缺少 EPP、严重低氧与补氧时才显示短提示。佩戴 EPP 时持续显示，低氧危险状态保留暖色警示。
- 默认受控供氧从 24 提高至 240 单位/秒（默认 Mk.I 空罐约 3 秒充满）；补给站灌装从 100 缩短至 20 tick（1 秒）。手持氧气罐使用时间仍为 2 秒。
- 灌装进度显示按实际总时长换算百分比。既有外部服务器配置仍由服主控制，需手动将 `refillPerSecond` 更新为 `240`；本地 run 和 GameTest 开发配置的旧默认值已更新。
- 验证：`gradlew.bat compileJava test --tests "*OxygenRulesTest" --console=plain` 成功（8 项氧气规则单测通过）；`gradlew.bat -PshipGameTests runGameTestServer --console=plain` 成功（17 项 GameTest 全部通过）；`git diff --check` 通过。头盔投影透明度及不同 GUI 缩放下观感仍需游戏内验收。


### 头盔投影一致性调整

- 氧量与所有短提示（包括未装备 EPP）共用曲面文字绘制：逐字沿弧线切线旋转，使用本地化视觉顺序，过长提示适配弧宽。
- 弧线及文字两端渐隐；第一人称转头时加入最大水平 1.5、垂直 1 个 GUI 像素的阻尼滞后，整组同步运动。暂停、菜单、第三人称及突变视角重置偏移。
- 本轮 `gradlew.bat compileJava --console=plain` 通过；尚未实机验证中英文警告、不同 GUI 缩放、快速转头和暂停恢复的观感。本轮只调整客户端绘制，未改补氧或耗氧规则。


### 氧气 HUD 警告标志

- 氧量读数左侧增加曲面投影三角叹号：余量 ≤30% 为琥珀色常亮，≤15% 或未装备 EPP 为橙红色；仅橙红色每秒一次柔和明暗闪烁，暗部仍保留轮廓。
- 图标与文字共用曲面变换和整体惯性位移，不占用提示文字行。保留既有 HUD 显示条件（未装备且在安全空气中不额外唤出 HUD）。
- 待游戏内检查低氧、未装备、补氧跨越阈值，以及中英文和 GUI 缩放下的图标间距与闪烁观感。


### HUD 连续曲面网格（替代逐字旋转方案）

- 文字、警告三角和氧量条先统一绘制到透明离屏纹理，再由 64×24 细分网格投影；同一个字符和图标内部也发生连续形变，包含向右上方弯曲及边缘透视压缩。
- 纹理随 GUI 缩放以 2–6 倍分辨率复用，网格坐标预计算；采用预乘透明度混合，避免透明边缘重复乘 alpha。退出连接时释放纹理；投影后恢复 framebuffer、视口、矩阵及混合等绘制状态。
- 保留轻微转头惯性、两端渐隐、琥珀色常亮和橙红色每秒一次闪烁规则。没有更改氧气玩法或网络数据。
- 验证：`gradlew.bat compileJava test --tests "*VisorSurfaceTest" --console=plain` 通过（4 项曲面测试：锚点、字形内部非线性弯曲、全网格不折叠/翻转、渐隐范围）；差异检查通过。
- 待实机：GPU 离屏合成、文字及三角形实际形变、亮暗背景透明边缘、中英文、GUI 缩放/窗口大小变化、菜单叠加、退出重进及转头舒适度；本轮未声称完成这些视觉验收。

### 未提交曲面 HUD 收尾检查（2026-09-18）

- 修复投影合成的低透明度截断：原版 `position_tex_color` 会丢弃 alpha < 0.1 的片元，
  使 alpha=20/255 的细光晕消失，alpha=40/255 的警告暗部在两端渐隐后出现截断。
  改用已依赖的 LDLib2 2.2.36.a `gui_texture` 着色器（仅丢弃完全透明片元），
  保留预乘透明度混合；已核对本地源码与实际依赖 JAR 的着色器及顶点格式。
- 渲染状态在 `GuiGraphics.flush()` 前保存，避免将其强制开启的深度测试误当作调用前状态。
  离屏绘制明确开启 RGBA 写入并使用加法混合方程，结束后恢复原颜色写入掩码、清屏颜色、
  RGB/alpha 混合方程及既有矩阵、framebuffer、视口、裁剪和深度等状态。
- 隐藏 OpenGL 窗口在 RTX 3060 Laptop GPU 上编译并运行实际依赖 JAR 中的着色器：
  5 档透明度 × 2 档渐隐 × 黑/白背景，共 20 组像素读回结果符合预乘 alpha 合成公式；
  旧着色器在同一测试中失败 6 组，新着色器全部通过，未产生 OpenGL 错误。
  本地验证程序：`build/VisorAlphaSmoke.java`；日志：`build/visor-alpha-smoke.log`。
- `gradlew.bat build --console=plain` 成功；545 项单元测试（含 4 项曲面几何测试），
  0 失败、0 错误、0 跳过；LDLib2 基线检查和 `git diff --check` 通过。
- GPU 像素测试只验证着色器合成，不等同于 Minecraft 客户端完整验收。
  中英文字形、不同 GUI 缩放、菜单叠加、退出重进和转头观感仍需游戏内确认。
  本次只收尾现有 HUD 改动，未扩展 R6/R7；保留原有设计文档及 output/tmp，未提交、未推送。

### HUD 惯性微调

- 按反馈小幅增强转头滞后：水平/垂直响应系数从 0.06/0.04 调至 0.065/0.045，
  最大偏移从 1.5/1 调至 1.8/1.2 个 GUI 像素；回正衰减系数从 10 调至 9，稍微延长余动。
- 保留右下角向右上方弯曲的曲面，以及暂停、菜单、第三人称和视角突变时的偏移重置。

### R6 交付与验证记录

- Mk.II 默认容量为 1080 单位（配置项 `mk2Capacity`），仅通过 Mk.I + 升级套件在工作站升级。
  升级不会免费补氧；补氧罐、供氧区、物品耐久条和同步 HUD 均读取当前机型容量。
  Mk.II 有独立三维模型，第三人称同步机型；当前提示明确说明 EVA 尚未开放。
- Heating I 提供 Cold I 防护；模块随 EPP 的 Data Component 保存与同步。
  只计算唯一 Active EPP 的合法模块，不叠加重复防护、不读取背包中的备用设备。
- 工作站使用实际 BlockPos 开屏，服务端复核菜单身份、方块、维度、距离、存活、旁观者和环境。
  各玩家独立三槽托盘：EPP / 模块 / 套件；原版拖放与 shift-click；关闭、断线或拆站关闭后返还输入。
  拆模块前必须空出模块托盘，重复请求不会复制材料；野外危险环境不允许维护。
- 寒冷暴露与氧气分离：25% 预警，50% 缓慢 I，75% 缓慢 II，100% 起每秒 1 点冻伤。
  足额防护或离开寒冷后立即停止伤害并恢复暴露；残留缓慢随暴露下降与短效果时限消退。
  创造/旁观者免除暴露；换装和重登不会清零暴露，死亡后重新开始。
- 曲面 HUD 增加低温暴露及当前防护/环境等级；NOVA 分级提醒带复位阈值；星图提示目标寒冷等级，
  不阻止导航或传送。Molten 高温与辐射仍只是环境数据，本轮没有启用对应伤害。
- 网络协议从 9 升至 10，同步机型与寒冷状态；联机客户端与服务器需要使用同版模组。

| 打印产物 | 材料 | Voxels | 时间 |
| --- | --- | --- | --- |
| EPP 维护工作站 | 基础电路板 ×4、铁锭 ×8、玻璃 ×2、金锭 ×2 | 75 | 5 秒 |
| Mk.II 升级套件 | 基础电路板 ×4、钨锭 ×8、铜锭 ×4、金锭 ×2 | 75 | 5 秒 |
| 加热模块 I | 基础电路板 ×2、铜锭 ×4、煤炭 ×4、铁锭 ×2 | 40 | 3 秒 |

验证结果：

- `gradlew.bat build --console=plain`：成功，552 项测试，0 失败、0 错误、0 跳过。
  日志：`build/epp-r6-build.log`。
- `gradlew.bat -PshipGameTests runGameTestServer --console=plain`：24 项全部通过，
  其中 7 项是本轮新增的 EPP 集成测试，覆盖升级保留名称/氧气、Mk.II 用罐、装拆及重复操作、
  不兼容内容自动返还、多人隔离、关闭返还、危险环境/远距离/已关闭/拆站拒绝、实际冻伤与安全区恢复、
  模块及暴露 NBT 往返和创造豁免。日志：`build/epp-r6-gametest.log`。
- 中英文均为 525 个键，无重复；模型、贴图、配方、掉落表引用检查通过。
  发布 JAR 包含新实现与资源，不包含 GameTest 类；`git diff --check` 通过。
- 集成测试使用环境查询扩展接口提供的测试区域，不能替代真实 Frozen 旅行或客户端联机验收。

游戏内验收顺序：

1. 在打印站制作维护工作站、升级套件和 Heating I，将工作站放在飞船舱内或 Lush 等安全环境。
2. G 页卸下 Mk.I，放入工作站左槽；套件放右槽并升级，确认名字与原氧量保留、容量变成 1080。
3. 模块放中槽并安装；悬停检查防护 I，取回并装备 Mk.II。确认第三人称外观变化。
4. 生存模式前往默认 Frozen：有 Heating I 时暴露保持 0；卸下 EPP 时暴露逐渐增加，
   HUD/NOVA 预警后先减速、后冻伤。Frozen 的可呼吸空气不会消耗氧气。
5. 重新装备带模块 EPP，确认立即停止积累/冻伤并恢复；回飞船亦恢复。实际节奏可通过配置调整。
6. 待实机：中英文界面和 GUI 缩放、按钮/拖放/携带物品关闭、两种背包外观、双行曲面 HUD、
   真实多人连接、退出重进及关服重启。以上未以自动测试替代人工验收。

R6 随后经用户确认，已提交并推送 `9b4dec0`；原有设计文档和 output/tmp 保留。

### R7a：EVA 出舱与返舱基础（2026-09-18）

基线为 `codex/epp-mechanisms` / `9b4dec0`。用户要求继续推进未完成内容，本轮落实独立可玩的 EVA 基础。

- 船外环境从真空/正常重力改为真空/零重力；沿用已有明确舱内区域的空气与人工重力。
  大气与重力分别判断：呼吸环境本身不会授予推进资格，普通星球移动不变。
- 唯一 Active Mk.II 内置推进，不占模块槽、不新增能量资源，不以氧量是否耗尽关闭推进。
  Mk.I、背包中的备用 Mk.II 或未装备时只能无动力漂浮。创造、旁观、乘坐、睡眠和水/岩浆中保留普通移动。
- WASD 随视角推进（含俯仰），Space 世界上升、Shift 世界下降；实际绑定显示在 HUD。
  每 tick 加速度 0.03，推进设备阻尼 0.88，无动力阻尼 0.98，总速上限 0.25 格/tick（5 格/秒），斜向输入归一化。
  松键渐停；菜单中不推进，输入超过 10 tick 未刷新即失效，客户端每 5 tick 或变化时发送输入。
- 环境、装备、输入资格由服务器复核；本地使用同一运动函数预测，沿用原版玩家位置同步与碰撞/移速检查。
  两个窄范围 Mixin 分别接管零重力中的 Player.travel、豁免该区域原版悬空踢出；不授予 mayfly/flying。
  回舱恢复原版重力；维度变化、死亡/重生不保留控制输入；EVA 状态不写入存档。
- 船外 HUD 显示返船方向箭头、距离、带正负号高差和控制提示，无推进器时警示。
  信标当前指向默认飞船传送器；既有向上曲面氧气 HUD 和惯性保持。
- 活着且非旁观的在线船员在飞船维度内、但不在明确舱内区域时，阻止起航且不扣燃料；
  航行中出舱则暂停航程，全员返舱后继续。客户端同步暂停标志并停止航程外推。
  其他星球上的玩家不阻止起航。返舱依据舱室，不使用距离球或整个飞船维度。
- Mk.II 在舱外使用返船键时提示沿信标飞回；无 EPP 或 Mk.I 的无动力漂浮玩家可用返船键紧急召回（见后续修正）。
- 网络协议 10 → 11；两端需同版。没有修改现有飞船结构，也没有新增气闸或密闭检测。

边界与后续：

- 这是 R7a，不是完整局部太空遭遇：不生成中继站、不占用固定遭遇坐标、不替换玩家方块。
  R7b 再落实 ShipEnvelope/EncounterEnvelope、动态安全锚点、代理切换、站点持久化与任务奖励。
- 舱内/返船范围仍是默认船舱；玩家自行扩建区域目前不自动获得空气、重力或返舱资格。
- 后续遭遇的离线参与者记录和卸载保护随遭遇生命周期实现；本轮只检查在线船员，未创建会卸载的遭遇。
- 未进行真实双客户端操控、延迟手感、信标视觉或旧存档重启验收；自动验证见下文。
- 本轮不提交、不推送。用户原有未跟踪文档与 output/tmp 保留。

R7a 验证结果：

- `gradlew.bat build --console=plain` 成功；562 项测试，0 失败、0 错误、0 跳过。
  新增覆盖视角与世界竖向、斜向归一化/限速/阻尼、输入时限、返舱边界、信标方位、网络往返及暂停/恢复外推。
  暂停恢复测试曾发现插值时钟沿用旧进度，修复后回归通过。日志 `build/epp-r7-build.log`。
- `gradlew.bat -PshipGameTests runGameTestServer --console=plain`：29 项通过（新增 5 项）。
  使用真实 ServerPlayer 与运行时 Mixin，验证 Active EPP 权限、零重力实际移动及回舱恢复、
  非法/超时/菜单/卸装输入、真实石墙碰撞、100 tick 移动连接不被悬空误踢且不泄露到普通重力。
  悬空连接测试补齐模拟玩家的区块追踪注册后通过。日志 `build/epp-r7-gametest.log`。
- 船员跃迁安全本轮验证了返舱判定单测、暂停同步与客户端时钟；未声称完成真实多客户端起航验收。
- 中英文本各 532 个键，无重复且键集合一致；发布 JAR 含 EVA/Mixin/配置且不含 GameTest 类。
  LDLib2 基线检查与 `git diff --check` 通过。

R7a 游戏内验收顺序：

1. 先在飞船内装备并补满 Mk.II，从舱室出口或自行打开的船壳通道离船。
2. 测试随视角前进、横移、上升/下降和松键减速；靠近船壳检查碰撞，核对信标方向、距离和高差。
3. 打开装备页时应停止推进；卸下 Mk.II 后只漂浮，重新装备后恢复控制。Mk.I 不提供推进。
4. 沿信标飞回舱内，确认重力恢复、氧气补充；常规远程返船键在舱外给出返航提示。
5. 双人联机：一人出舱时另一人请求跃迁，确认拒绝且燃料不变；全员返舱后可起航。
   航行中再次出舱时进度暂停，返舱后继续；随后检查死亡/重生、重连和不同维度移动。


### R7a 误出舱自救修正

- 按用户反馈补齐无 EPP / Mk.I 的自救：船外无动力漂浮时，返船键（默认 H，支持改键）直接请求紧急召回；HUD 显示实际按键。
- 服务端检查玩家存活、飞船维度、舱外位置和无推进状态。救援不依赖核心上线，避免序章阶段误出舱后只能等死。
- 优先回到传送器落点；被占用时搜索明确舱内的其他安全落脚处，检查站立碰撞、空气、液体和脚下支撑。
  不覆盖方块；若整个舱内无安全落点，明确告知失败。召回清除速度、坠落距离和输入，保留装备、物品及氧量。
- Mk.II 仍正常飞回；本版按当前 Active EPP 判定，卸下 Mk.II 也能使用这一救援兜底，不引入死亡、丢物或资源惩罚。
- 新增 GameTest 覆盖裸装/Mk.I 召回落点、速度与坠落清零、保留装备和氧量、阻塞/舱外落点拒绝、其他维度无救援资格。
  30 项 GameTest 全部通过，日志 `build/epp-recall-gametest.log`。实际飞船按键/HUD 仍待游戏内验收。
- 最终 `gradlew.bat build --console=plain` 通过，562 项单元测试无失败；中英文各 534 个键、无重复且一致，差异检查通过。日志 `build/epp-recall-build.log`。未提交、未推送。

### R7b：首个废弃中继站遭遇闭环（2026-09-18）

基线为 `codex/epp-mechanisms` / `97558c9`（R7a 与紧急召回已提交推送）。本轮继续落实局部太空遭遇。

- 星图新增独立中继站信号、接近/离开操作、任务状态、燃料提示与舰外船员等待提示；不加入星体目录。
- 使用飞船连接结构扫描、方向投影、9 个候选方向、24 格 EVA 间距和 8 格安全边界选择锚点。
  实体化前再次检查实际方块和实体；范围过大或没有安全空间时拒绝生成，不覆盖玩家建筑。
- 接近过程为 160 tick；提供远景代理，实体区域未加载时保留代理提示；EVA HUD 增加中继站方位、距离和高差。
- 新增 31×17×31 废弃中继站模板与生成脚本，包含破损入口、探索空间、氧气缓存、数据核心和科技物资。
  取得数据核心并全员返舱后记录任务完成。
- 保存玩家方块修改和容器库存，离开后再次访问恢复快照，已取走的奖励不补刷。
  建筑触及保存边界、站内仍有实体或有在线/离线舰外船员时阻止清理；玩家死亡和返舱后清理船员记录。
- 放置/移除使用持久化事务标记，重启尝试保守恢复；方块或库存冲突进入 ERROR 并保留现场。
  管理员处理冲突后可执行 `/starboundmc_relay_recover`。库存比较包含额外新增的物品，避免清理时丢失玩家物资。
- 网络协议 11 → 12；客户端和服务器必须使用同版。

最终自动验证：

- `gradlew.bat build --console=plain` 成功；569 项单元测试，0 失败、0 错误、0 跳过。
  日志：`build/relay-final-build.log`。
- `gradlew.bat -PshipGameTests runGameTestServer --console=plain`：34 项全部通过。
  新增遭遇测试覆盖编辑/容器保存与重访、奖励不补刷、晚出现的障碍、恢复幂等、修改及新增库存冲突、外部方块冲突和保存边界。
  日志：`build/relay-final-gametest.log`。
- 中英文均为 565 个键，无重复，键集合和格式占位符数量一致；发布 JAR 包含中继站模板、遭遇/网络/客户端类，不含 GameTest 类。
- LDLib2 基线检查通过；`git diff --check` 通过。

游戏内待验收：

1. 在引擎可用的飞船星图中接近中继站，检查中英文面板、GUI 缩放、代理到实体的切换和低视距表现。
2. 穿戴并充满 Mk.II，沿信标出舱，找到氧气缓存和数据核心，再返回飞船确认任务完成。
3. 修改站内方块、存入物品、取走奖励，离开并重访，确认保留修改且不会补刷。
4. 双人联机检查舰外等待、登出/重连、死亡和返舱；检查阻止撤离时不会消失站点或扣除航路燃料。
5. 旧存档首次发现、正常关服重启、接近中重启和事务中断恢复仍需实机验收；自动测试不替代这些检查。

本轮未提交、未推送；原有未跟踪设计文档及 output/tmp 保留。
后续 R8 仍为 Mk.III/IV、更多环境防护、Mobility Rig/Jump Thruster 与 Barren 高级科技进度。

### R7b 接近/抵达崩溃修正

- 实机日志确认 RelayEncounter 广播使用 relay 翻译前缀，违反 NovaBroadcastPacket 只接受 nova 前缀的约束，接近请求抛错并在抵达 tick 导致服务端退出。
- 广播改用 message.starboundmc.nova.relay.*，补齐 8 个双语键；保留普通操作提示。广播包在玩家循环前校验，无玩家测试也能发现前缀错误。
- 新增回归测试扫描实际广播调用，验证每条消息的包构造、编解码及中英文翻译存在。
- build 通过，570 项单元测试、34 项 GameTest 全部通过；日志 relay-crashfix-build.log / relay-crashfix-gametest.log。差异检查通过。修复后的实际接近/抵达仍待重启游戏复测；未修改玩家存档，未提交推送。

### R8a：Mk.III 远征背包基础（2026-09-18，待提交）

- 新增 EPP Mk.III 与升级套件。Mk.II + Mk.III 套件在维护工作站升级，保留自定义名称、氧气和已安装模块。
- Mk.III 容量默认 1440，保留 EVA 推进，开放 2 个模块槽并接受 Tier II 模块；工作站安装操作追加模块，不覆盖已有模块。
- 新增双语物品、模型、配方和资源回归，第三人称背包渲染与装备页显示 Mk.III。
- 自动验证：build 成功；35 项 GameTest 全部通过，包含 Mk.III 升级、模块保留和第二槽安装。日志 `build/r8-mk3-build.log`、`build/r8-mk3-gametest.log`。
- Mk.IV、Mobility Rig/Jump Thruster、Heat/Radiation 新伤害和高级模块仍未实现。

### HUD 统一与安全环境淡出

- 顶部飞船/中继站导航与操作提示复用头盔曲面投影、边缘透明及轻微惯性；双目标并排显示。
- 导航箭头使用带明暗侧面的三维几何投影，表达水平朝向与世界高差；目标在身后时显示本地化“后方”提示。
- 可呼吸且无窒息残留、未补氧时，氧气 HUD 保留 1.5 秒后在 0.8 秒内淡出；真空、窒息残留或补氧时在 0.15 秒内恢复。
  寒冷提示独立保留，游戏暂停不推进淡出计时。透明度作用于最终预乘透明纹理，避免文字和光晕出现黑边。
- 待实机确认：中英文/GUI 缩放、双目标方向与高差、目标在身后、入舱补氧及补满淡出、Lush 安全空气和 Frozen 寒冷提示。

HUD 可读性修正：实机截图发现顶部操作提示仅横向拉宽导致文字失真，整数扫描线箭头导致阶梯边缘。
顶部改用 128/360 宽的独立纹理与对称轻曲率，文字始终等比缩放；箭头改为浮点三角面，纹理使用两倍屏幕采样后线性缩小。
沿用 LDLib2 HUD 生命周期，断开时释放新增投影纹理。`build/hud-legibility-build.log` 构建通过，新增几何测试验证字宽/字高不拉伸与曲率斜率上限；实际视觉仍待复测。
后续导航修正：飞船与中继站合并为同一张 272 宽纹理并一次曲面投影，消除两个独立弧面。
修复旧箭头左右轴反转，使用摄像机水平朝向；采用薄挤出平面箭头（宽尖端、窄尾杆），前/右/后/左分别指上/右/下/左，高差独立数值显示。
新增多朝向回归测试验证实际箭头坐标与目标方向一致；日志 `build/hud-navigation-build.log`。视觉待实机复测。

2026-09-19 按用户反馈将顶部目标箭头替换为方位盘与 AR 引导：
- 顶部显示 N/E/S/W、角度刻度和中心航向，沿用轻微头盔曲面。
- 通过世界渲染事件的真实视图/投影矩阵将飞船与中继站位置映射到 HUD；视野内为空心菱形，视野外沿安全边缘显示方向，身后附加“后方”。
- AR 标记保留距离、高差与颜色区分，近邻标签错行；标记不叠加头盔惯性，避免与世界目标错位。补氧与氧气淡出行为保留。
- 新增投影回归覆盖正前方、右上方、视野外、身后、近零深度；构建日志 `build/hud-ar-build.log`。实机需检查转头/抬头、FOV、GUI 缩放及身后目标过渡。
- 用户已确认 AR 视觉；交付前按反馈将菱形半径 7 → 6、边缘指引长度 9 → 7.5、文字缩放 0.8 → 0.68，图文间距同步收紧。最终验证日志 `build/hud-ar-final-build.log`。

### EVA 航程扩充（2026-09-19）

- HUD 已经用户确认并提交推送 `43bea04`。本轮按先前确认的 HUD 后续顺序扩充船站航程。
- 默认船体/站体包络间隙从 24 格提升到 80 格。现有 SERVER 配置新增 `relayEvaGap`（24–120），随存档配置；中心距上限仍为 160 格。
- 候选搜索先检查全部 9 个方向的首选间隙，受阻时每次向外增加 16 格；所有候选在方块取整后再次检查中心距上限。
  给取整留出 1 格余量，避免斜向候选压缩净空间隙。实际方块、实体、世界边界与保存保护仍由原路径检查。
- 活动中继站不原地移动；修改配置或升级旧存档后，全员返舱、正常离开站点，再接近时应用新间隙。玩家修改与库存继续使用站点快照恢复。
- 以 5 格/秒速度估算，80 格净空单程至少约 16 秒，实际进出舱、绕行与靠站更长；氧气、速度与紧急召回规则不变。
- 此阶段未扩展至 512–1024 格范围，亦未替换远景代理美术；低视距代理切换与实际往返节奏仍待实机体验。
- 验证日志：`build/eva-range-build.log`、`build/eva-range-gametest.log`。新增几何回归覆盖不同配置的净空距离、向外避障与严格中心距上限。
- 后续：改进远景代理与低视距切换、星图空间 POI 和多站点内容；高级防护闭环仍需配套模块/制造/预警后实施。80 格 EVA 航程与往返节奏已通过用户实机视觉确认。

### 中继站远景代理与低视距交接（2026-09-19）

- 远景代理按真实远距目标计算方向与角尺寸；当目标超出客户端方块渲染距离时，将代理等比缩放到视距内，避免 80 格航程开始时被远裁剪面或区块雾直接隐藏。
- 接近进度仍使用减速曲线并最终收敛到动态锚点。代理改为分层船体、连接臂、双太阳能板、天线与航行灯的简化轮廓，便于远距离辨认。
- ACTIVE 阶段检查站体占用的全部区块，不再只检查中心点。完整站体区块持续就绪 8 tick 后，代理用 16 tick 渐隐；区块卸载时用 4 tick 恢复，降低低视距移动中的空白和闪现。
- 新增纯几何/过渡回归，覆盖低视距投影的角尺寸守恒、最终锚点收敛和双向渐变。完整 build 通过，580 项单元测试无失败；日志 `build/relay-proxy-build.log`。低视距、接近及渐隐效果仍待实机视觉确认。

### 中继站造型调整（2026-09-19）

- 用户已确认上一轮远景交接表现，要求同时改进远景模型与近处站体造型。
- 模板增加分段抬高舱顶、外露装甲肋条、带黄色识别灯的登临口、四组太阳能电池翼、阶梯凹形通信碟和信号桅杆；四舱室保留原有入口、奖励和工作设施，补充控制台、灯光和检修细节。
- 远景代理对齐新模板的轮廓、尺寸和部件位置，增加电池分格及各面明暗。保留既有低视距投影与渐隐交接。
- 生成器检查全部方块不触及保存边界、入口保持三格宽两格高、四舱室与两处奖励均可步行到达。
- 新模板只影响首次生成的站点；已有站点及其玩家修改继续从保存快照恢复，不自动覆盖。用户已确认新造型的游戏内视觉效果。
- 验证：模板生成及通行断言通过，完整 build 成功，580 项单元测试零失败，差异检查通过；构建日志 `build/relay-model-build.log`。用户已授权提交、推送并继续下一项。

### 星图中继站兴趣点（2026-09-19）

- 已确认的中继站造型与远景交接已提交、推送：`672d981`。
- 中继站作为独立空间兴趣点显示在银河、所属星系和所属行星家族视图；沿所属天体的插值位置显示站体符号，使用同一坐标绘制、命中和选中框。未发现或尚未解锁的星系不会露出可选站点。
- 原左下角操作框收为“定位中继站信号”入口。选中站点后使用星图统一详情面板，显示所属天体、航路燃料、站点阶段、真空/EVA 装备建议与探索进度，执行接近或离开。
- 客户端按钮检查船员返舱、跃迁、燃料和引擎状态；离开站点不额外要求燃料或修复引擎。实际操作继续由原服务端菜单、船员安全、锚点及快照流程复核。
- 本步接入已有单站点，不修改天体目录、网络协议或存档格式；随机多站点、站型池和独立站点持久化留待后续。
- 用户已确认星图兴趣点视觉，授权提交、推送并继续下一项；真实多人流程仍需单独验收。
- 自动验证：LDLib2 基线通过；完整 build 成功，584 项单元测试零失败；新增覆盖所属星系/行星家族、地图偏移、接近条件和无燃料离开规则。差异检查通过，日志 `build/relay-starmap-build.log`。未进行游戏内视觉和交互验收。

### R8b 高温防护闭环（2026-09-19）

- 已确认的星图兴趣点版本提交、推送为 `14a4f7c`。用户将多站点探索延后，本轮未修改多站点实现，转向高温防护。
- 新增 Cooling I 制冷模块、独立像素图标与打印配方：基础电路板 2、铜锭 4、玻璃 4、铁锭 2、40 Voxels、3 秒。沿用安全区域维护工作站安装/拆除，Mk.II 单槽选择制热或制冷，Mk.III 可同时安装两者。
- 默认 Molten 仍可呼吸，高温从预留 III 调整为 I，与当前可制作的 Cooling I 配套。自定义高阶环境继续按防护差额计算暴露；不增加装备传送门禁。
- 热暴露独立于氧气和冷暴露：每秒每阶缺口 +2，安全或防护足额每秒 -5，25/50/75 预警，50 起虚弱，100 且防护不足时每秒 1 点热损伤。创造/旁观免除暴露，存档保存热暴露及预警滞回。
- 热损伤使用独立 damage type，忽略普通护甲且不点燃玩家。制冷模块不改变熔岩/火焰规则。HUD 独立显示高温/防护与热暴露，氧气淡出不隐藏高温提示；冷、热、氧气共存时向上扩展避免底部重叠。
- 双语 NOVA 分级预警、装备提示和星图制冷需求已补齐。EPP 快照扩展热暴露字段，网络版本 12 → 13，联机两端需使用同一版。
- 自动验证：LDLib2 基线与差异检查通过，完整 build 成功，585 项单元测试及 37 项 GameTest 全部通过；日志 `build/heat-build.log`、`build/heat-gametest.log`。新增回归覆盖冷暖模块共存、工作站安装、真实玩家热损伤、暴露保存恢复、制冷与氧气独立、星图提示和快照编解码。
- 待实机：Cooling I 制造/安装与图标，Molten 未防护暴露及防护后恢复，热提示与氧气淡出的组合，中英文星图/GUI 缩放，实际重连和死亡行为。新一轮尚未提交、推送。

### 温度防护简化（2026-09-19）

- 按用户最新决定，Frozen 的寒冷与 Molten 的炎热不再区分等级，替代上文温度防护差额规则。有效装备中安装加热/制冷模块即可完整抵御对应环境，重复安装不叠加。
- 未防护时按固定速率累积暴露；安装对应模块后停止累积及伤害，已有暴露逐步恢复。原有氧气、暴露预警和症状规则保留。
- 保留既有物品 ID、存档及网络数值字段以兼容旧数据；温度字段仅按零/非零判断，旧的高等级环境也不要求更高级温度模块。辐射预留等级与 EPP 机型/槽位规则不变。
- 去掉加热/制冷模块显示名的 I 和温度模块等级提示；HUD 改为“防护已生效 / 需要对应模块 / 暴露恢复中”，星图与物品提示同步去等级。
- 回归覆盖旧高等级温度环境下固定暴露速率、任意对应模块完整防护及双模块共存。实机视觉待确认，未提交、推送。
- 验证：完整 build 成功，585 项单元测试、37 项 GameTest 全部通过；差异检查通过。日志 `build/thermal-binary-build.log`、`build/thermal-binary-gametest.log`。

### 防护 HUD 与打印浏览调整（2026-09-19）

- 对应温度防护生效后，冷热暴露条各自沿用氧气 HUD 的淡出节奏：约 1.5 秒停留、0.8 秒淡出，失去防护时约 0.15 秒恢复。已有暴露继续在服务端恢复，氧气和另一类环境危险独立显示。
- 修复 12 个打印配方仍使用旧 `voxel_cost`、实际未扣体素的问题，将原定 10–120 体素成本迁入材料列表；物质枪模块原有 100 体素成本保留。队列沿用钱包预扣与取消返还流程。
- 打印机增加全部、装备、零部件、机器、建材、其他分类；通过产物物品标签归类，未分类的数据包产物仍能在其他/全部找到。切换分类更新已有行并校正选择和详情。
- 左侧物品列表向下延伸至背包底部，列表视口从 103 增至 167；背包槽位、打印详情及紧凑模式队列布局保留。
- 验证：完整 build 与 586 项单元测试通过；GameTest 服务端运行通过，新增真实配方加载、120 体素成本解析和分类标签/未分类回退检查。日志 `build/hud-printing-build.log`、`build/hud-printing-gametest.log`。
- 待用户实机确认冷热防护淡出/拆除重现、中英文分类下拉、列表滚动与紧凑布局、打印扣费及取消返还。尚未提交、推送。
