# 普通机器界面 LDLib2 迁移计划

> 状态：进行中
>
> 分支：`main`
>
> 建立日期：2026-08-27

## 目标与设计边界

逐个把仍使用原版手绘或旧按钮组件的普通功能方块界面迁移到 LDLib2。普通机器不是星图：
界面应简单直接，尺寸和信息占比接近旧界面，只利用 LDLib2 精修间距、边框、焦点、悬停、
滚动、按钮状态和物品交互。

- 每个页面可有独立视觉语言，不强制沿用传送器配色或装饰。
- 共享层只提供 `ModularUI` 生命周期、布局和稳定控件，不建立统一的大型仪表盘外壳。
- 不借 UI 迁移修改服务端业务、菜单槽位语义、网络权限或注册 ID。
- 每个方块独立实现、测试、客户端验证和提交；未经确认不批量替换所有页面。
- 旧 `ship_console` 为存档兼容保留且已废弃，不纳入迁移。

## 当前进度

### M0：基础能力

- [x] 引入并固定 LDLib2 `2.2.36.a` 客户端依赖和模组元数据依赖。
- [x] 建立可复用的 `StarboundModularScreen` 生命周期桥接。
- [x] 建立机器界面 LSS 入口，同时允许页面使用专属选择器覆盖。

### M1：传送器

- [x] 用 `ScrollerView`、`TextField`、`Button`、`Label` 和 `UIElement` 替换旧手绘界面。
- [x] 保持紧凑单列布局：界面 `184×178`，接近旧版约 `176×166` 的占比。
- [x] 恢复目的地点击、滚轮、命名、保存/Enter 和 Esc 关闭。
- [x] 长节点名称使用独立名称区域、悬停滚动和完整 tooltip，不扩大面板。
- [x] 完成传送器专属的紧凑飞船设备风第一版，不影响其他页面主题。
- [x] 自动测试通过，客户端完成当前版本视觉确认。
- [x] `main` 阶段提交：`e9006fb`（`feat(ui): migrate teleporter screen to LDLib2`）。

### M2：飞船储物箱（下一项）

- [ ] 盘点 `ShipCrateMenu` 的 54 格容器槽和玩家背包槽位映射。
- [ ] 使用 LDLib2 `ItemSlot` / `InventorySlots` 构建紧凑界面，尺寸接近原储物箱。
- [ ] 保持拖放、双击合并、Shift 快捷移动、热键交换和物品 tooltip 行为。
- [ ] 验证关闭、重复打开、容器失效和服务器同步，不复制客户端物品状态。
- [ ] 完整自动测试后交由用户进行功能与视觉核验，再单独提交。

### M3：钛合金熔炉

- [ ] 迁移输入、燃料、输出槽和烧制/燃料进度显示。
- [ ] 保持配方、燃料消耗、Shift 移动规则与服务端菜单数据不变。
- [ ] 使用紧凑机器面板，视觉主题在实现前单独确定。

### M4：燃料控制器

- [ ] 迁移燃料槽、共享油量显示、添加燃料操作和错误/禁用状态。
- [ ] 明确区分物品燃料值、当前油量和油箱上限，不扩大为大型控制台。

### M5：物质枪升级工作台

- [ ] 迁移物质枪、组件槽、四条升级路线、成本和禁用原因。
- [ ] 保持 Data Components、服务端校验及升级网络请求不变。
- [ ] 对较多信息使用清晰分组，但仍保持接近旧界面占比。

### M6：收口

- [ ] 所有消费者迁移后，审计并删除不再使用的 `PixelButton`、`SciFiButton`、`UiStyle`
      和旧手绘辅助代码；兼容代码仍有调用时不得提前删除。
- [ ] 运行完整 `test`、`build`、客户端资源重载和专用服务器启动检查。
- [ ] 更新 README、归档本计划，并记录每个页面的最终提交和视觉验收结果。

## 每页质量门

1. 编译和相关单元测试通过。
2. 菜单数据、槽位索引、网络操作和服务端权限与迁移前一致。
3. GUI Scale 与常见窗口比例下不裁切、不重叠；键盘焦点和鼠标滚动正确。
4. 用户亲自完成视觉和关键交互验证；除非明确要求，不使用截图分析消耗额外验证时间。
5. 只提交项目源码和资源；`.agents/`、`LDLib2/`、临时截图及运行文件始终排除。
