# 星图重绘 TODO

基线提交：`b79fd73`（LDLib2 组件化星图、公转平滑与卫星间距修复）。

本清单对应 `codex/starmap-redraw-opt` 分支的后续工作。每项完成后应先通过
自动测试，再进行对应的客户端视觉核验。

## 范围约定

- 矿物分布暂缓实现。当前星球维度仍是空壳，暂不新增矿物数据模型、生成逻辑或
  虚假展示字段；待星球维度和资源生成模型落地后再单独排期。
- `LDLib2/` 本地下载目录、临时截图和运行日志不纳入版本库。
- 跃迁继续使用现有服务端权威请求，星图 UI 不创建第二套航行状态。

## P0：交互闭环

- [x] 统一三级导航规则：恒星单击选中、双击进入，信息板也提供“进入恒星系”按钮；
      行星选中后进入行星系统；
      右键逐级返回，深空层右键不关闭，Esc 关闭星图。
- [x] 深空恒星使用抽象十字星芒；恒星系中央恒星可选中并展示无跃迁按钮的信息板。
- [x] 空白左键取消选中并隐藏信息板；按钮点击不得被场景层拦截。
- [x] 恒星系中点击卫星的可视点或扩大命中区时归属并选中父行星，但不能独立进入卫星；
      行星系统中卫星仍可独立选中。
- [x] 建立不可用目标原因模型，并让文案、按钮禁用态和视觉状态同步：当前目标、不可达、
      燃料不足、跃迁中等情况都要能解释原因。
- [x] 信息板根据目标实时位置自动避让：优先放在可用侧，靠近边缘时翻转并夹紧，不能
      遮挡目标、轨道或主要航道；公转期间持续跟随。
- [x] 回归公转与命中一致性：绘制和命中共用同一 `partialTick` 相位，移动目标不可出现
      视觉位置与命中区域错位。

以上已完成项于 2026-08-25 通过客户端视觉核验；选中角标、超空间航道、公转轨道的
连续绘制，以及信息板随目标移动和边缘避让效果均已确认。

## P1：交互与数据完善

- [x] 加入有限滚轮缩放、左键拖拽平移、重置视野和聚焦当前目标入口；缩放以鼠标为锚点，
      设置最小/最大边界且不破坏命中逻辑。
- [x] 将深空星域改为可配置数据图：恒星稳定 ID、位置、描述、类型、天体数量和航道边，
      不再按列表顺序硬连；为解锁/不可达状态预留字段。
- [x] 盘点并补齐现有星球/卫星缩略图 ResourceLocation，统一尺寸、裁切和选中态；缺图
      使用明确且稳定的 SDF fallback，不能产生 missing texture。
- [x] 为进入/返回、选中高亮和信息板出现/隐藏加入 120–200ms 的 LDLib2 过渡动画；动画
      只改变视觉，不改变状态机和命中区域。
- [x] 继续 LDLib2 绘制迁移：将星空、轨道、虚线航道等剩余 `GuiGraphics` 绘制拆到场景/
      装饰子元素或纹理层，根元素只保留状态编排与事件协调。

视图交互项于 2026-08-26 通过客户端视觉核验；三级星图的鼠标锚点缩放、空白拖拽、
选中目标聚焦、默认视野重置，以及移动目标的绘制/命中/信息板同步均已确认。

星域数据图项于 2026-08-26 完成自动验证；内置资源、显式航道拓扑、稳定 ID、重复边/
未知端点校验、连通性和回退图均有测试覆盖，默认配置不改变已验收的星域构图。

星球/卫星缩略图项于 2026-08-26 通过客户端视觉核验；普通图与聚焦图会在使用前验证
资源，缺失或非法 ResourceLocation 使用稳定的 SDF 天体回退，不再产生 missing texture。

LDLib2 过渡动画项于 2026-08-26 通过客户端视觉核验；层级切换、选中角标和信息板显隐
均使用短促动画，且全屏过渡避开 opacity 离屏层，不再产生黑屏。

剩余绘制迁移于 2026-08-26 完成结构实现与自动测试：星空、网格、航道、公转轨道和
选择角标已离开根元素，分别由场景、选择覆盖层和共用矢量绘制组件负责；天体节点改为
单一路径命中；该项于 2026-08-26 通过客户端视觉与交互核验。

## P2：质量门

- [x] 验证 GUI Scale 1/2/3/4/Auto、16:9/16:10/4:3、窄窗口和超宽窗口；覆盖边缘目标、
      密集卫星、重复打开、重进世界和联机场景。
- [x] 补充导航状态、不可用原因、缩放边界和星域图连通性的单元测试。
- [x] 完成三级导航、密集卫星、边缘目标和缩放/拖拽关键路径的客户端视觉 smoke test；
      根据用户验收决定，不保留截图。
- [x] 检查长时间公转、切屏重开和重复打开星图时的性能、监听器生命周期和服务端权威跃迁
      状态，避免内存或事件泄漏。
- [x] 确认新星图终端通过 `WarpControlMenu` 权限边界后，跃迁按钮可以实际启动服务端跃迁。

自动化质量门于 2026-08-26 完成：操作可用性与禁用原因改为单一纯逻辑判定，并覆盖导航、
缩放边界和星域连通性；公转相位归一化后在长时间运行下仍保留 sub-tick 精度；信息板、
选框和转场动画随 LDLib2 元素树移除时取消订阅，重复初始化继续通过 `disposeModularUi()`
释放旧界面。服务端跃迁入口、菜单有效性、目的地与燃料复核继续由 Stage 7 权威性测试覆盖。
`gradlew.bat test build --no-daemon` 通过，共 189 项测试、0 失败。

GUI Scale、宽高比和关键路径视觉矩阵于 2026-08-26 通过用户核验。核验同时发现新星图终端
的跃迁请求被旧 `ShipConsoleMenu` 专属校验静默拦截；现已让旧控制台与新终端共同实现
`WarpControlMenu` 权限边界，仍在网络处理层和实际动作层分别复核 `stillValid`；修复于
2026-08-26 通过客户端端到端跃迁核验。

## 建议顺序

按 `P0-1 → P0-2 → P0-3 → P0-4 → P0-5 → P0-6 → P1 → P2` 推进。每完成一项，记录
测试命令、客户端截图和未覆盖的环境，再进入下一项。

## P3：交付收口

- [x] 将静态星空和校准网格缓存为尺寸感知纹理；仅在视口尺寸改变时重建，并在 LDLib2
      元素移除时释放纹理，避免每帧重新创建随机数和逐点绘制。
- [x] 将需求与优化文档同步到最终组件结构和验收状态，明确矿物分布仍为唯一延期字段。
- [x] 运行完整测试与构建，审计分支相对 `main` 的提交范围和本地未跟踪资源。
- [x] 客户端确认调整窗口尺寸、关闭重开后星空密度、网格和层级内容视觉不变。

P3 自动化检查于 2026-08-26 完成：星空使用固定种子生成并按视口尺寸缓存为
`DynamicTexture`，窗口尺寸不变时不再逐帧生成星点；尺寸变化和元素移除会释放旧纹理。
`gradlew.bat test build --no-daemon` 通过，共 192 项测试、0 失败。分支差异已审计，改动
局限于星图终端及其构建、资源、文档和测试边界；`.agents/`、`LDLib2/` 和临时截图继续
排除在提交外。

P3 星空缓存于 2026-08-26 通过客户端视觉核验：默认窗口、调整尺寸和关闭重开后，
星点密度、校准网格、航道、轨道与星体均正常显示。

## P4：舰桥整合与跃迁反馈

- [x] 跃迁期间在星图画面下方中央显示“跃迁中”提示；提示使用独立 LDLib2 `Label`，仅对
      文字颜色 Alpha 做连续呼吸变化，不修改元素或任何全屏层透明度。
- [x] 保留旧 `ship_console` ID、物品和菜单以兼容已有存档，但在中英文方块名和容器名中
      标记为“已废弃”；程序生成的新世界飞船改放两台 `starmap_terminal`。
- [x] 为新星图导航台增加四个水平朝向、放置方向、旋转/镜像行为及四向 blockstate 模型映射。
- [x] 补充跃迁提示曲线与导航台朝向回归测试，并通过完整测试与构建。
- [x] 客户端视觉核验：确认跃迁提示位置和闪烁节奏、新世界飞船设备替换，以及导航台
      四向放置时的模型朝向。
- [x] 对拥有卫星的行星，在恒星系与行星系统信息板中增加“拥有 x 颗卫星”数据行；按
      `parentEntryId` 统计直接卫星，无卫星行星和卫星自身不显示该行。
- [ ] 客户端视觉核验卫星数量行及信息板增高后的排版。

P4 自动化实现于 2026-08-26 完成。静态资源检查确认程序飞船模板包含两台新导航台且不再
放置旧驾驶台；中英文资源与四向 blockstate 均可解析。`gradlew.bat test build --no-daemon`
最新通过，共 194 项测试、0 失败。当前等待卫星数量行的客户端视觉核验，核验前不提交本轮改动。

首次视觉核验发现 LDLib2 的元素级 opacity 会把跃迁 Label 的整条离屏合成盒显示成黑条；
现已改为只改变字形 Alpha，并把 Label 收窄到画面中央。同期确认静态星空的稀有 flare 分支
会生成与恒星节点相似的 7 像素十字亮星；该分支已移除，普通背景星最大限制为紧凑 2×2 星点。
两项修复及原 P4 舰桥整合内容已于 2026-08-26 通过客户端核验。
