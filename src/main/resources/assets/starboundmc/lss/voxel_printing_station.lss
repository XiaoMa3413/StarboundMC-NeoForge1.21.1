// Graphite fabrication console. One-pixel relief is drawn by PrinterUiSkin;
// this sheet owns fills, text and interaction colours. Geometry stays unchanged.
.machine-inventory-screen { background: rect(#A0050910); }

.voxel-printing-shell {
  background: group(rect(#101A22), border(-1, #344B55));
  overflow-clip: true;
}

.machine-inventory-header { background: rect(#16232D); }
.voxel-printing-rail { background: #63D8D4; }

.machine-inventory-title {
  font-size: 9;
  text-color: #D8EDF0;
  text-shadow: false;
}

.machine-status {
  font-size: 6;
  text-color: #7596A0;
  text-shadow: false;
}

.voxel-printing-wallet {
  font-size: 7;
  text-color: #9DBECA;
  text-shadow: false;
}

.voxel-printing-recipe-pane,
.voxel-printing-detail-pane,
.voxel-printing-queue-pane {
  background: rect(#0D171F);
}

.voxel-pane-title,
.machine-inventory-caption {
  font-size: 7;
  text-color: #88A6AF;
  text-shadow: false;
}

.voxel-printing-category {
  background: rect(#1A2A35);
  focus-overlay: border(-1, #63D8D4);
}

.voxel-printing-category:hover { background: rect(#213642); }

.voxel-category-dialog {
  background: group(rect(#101D26), border(-1, #526975));
}

.voxel-category-label {
  font-size: 7;
  text-color: #C9E0E4;
  text-shadow: false;
}

.voxel-recipe-list .__scroller_view_vertical-scroller__,
.voxel-print-queue-list .__scroller_view_vertical-scroller__,
.sb-material-requirements .__scroller_view_vertical-scroller__ {
  width: 3;
  margin-left: 2;
}

.voxel-recipe-list .__scroller_scroll_container__,
.voxel-print-queue-list .__scroller_scroll_container__,
.sb-material-requirements .__scroller_scroll_container__ {
  background: rect(#081116);
  padding-all: 0;
}

.voxel-recipe-list .__scroller_scroll_bar__,
.voxel-print-queue-list .__scroller_scroll_bar__,
.sb-material-requirements .__scroller_scroll_bar__ {
  base-background: rect(#506874);
  hover-background: rect(#80A4AF);
  pressed-background: rect(#63D8D4);
}

/* Recessed input with an always-visible baseline; focus stays inside its existing bounds. */
.sb-recipe-search:host {
  background: rect(#081116);
  focus-overlay: border(-1, #63D8D4);
  padding-horizontal: 3;
}
.sb-recipe-search:hover { background: rect(#0D1D27); }
.sb-recipe-search {
  font-size: 6;
  text-color: #CFE5E8;
  text-shadow: false;
}

/* Catalogue row: a browser entry, not a button. No border and no panel of its own — selection is a
   subtle fill plus the accent rail, so a chosen row stays legible once the pointer moves away
   without lighting up like a card. Availability is a separate, quieter rail tone. */
.sb-recipe-browser {
  background: rect(#00000000);
}
.sb-recipe-row:host {
  base-background: rect(#00000000);
  hover-background: rect(#15252F);
  pressed-background: rect(#1B303A);
}
.sb-recipe-row.sb-selected:host {
  base-background: rect(#17343E);
  hover-background: rect(#1E424C);
  pressed-background: rect(#12303A);
}
.sb-recipe-rail { background: #00000000; }
.sb-recipe-row.sb-craftable .sb-recipe-rail { background: #35545C; }
.sb-recipe-row.sb-selected .sb-recipe-rail { background: #63D8D4; }
.sb-recipe-row.sb-unavailable .sb-recipe-name { text-color: #9AAFB9; }
.sb-recipe-row.sb-unavailable .sb-recipe-amount { text-color: #7F96A2; }
/* Selection wins even when the chosen recipe has missing ingredients. */
.sb-recipe-row.sb-selected .sb-recipe-name,
.sb-recipe-row.sb-selected.sb-unavailable .sb-recipe-name { text-color: #E5F3F5; }
.sb-recipe-row.sb-selected .sb-recipe-amount,
.sb-recipe-row.sb-selected.sb-unavailable .sb-recipe-amount { text-color: #A9C7CE; }
.sb-recipe-name { font-size: 7; text-color: #CFE5E8; text-shadow: false; }
.sb-recipe-amount { font-size: 5; text-color: #6F929C; text-shadow: false; }

.voxel-recipe-empty,
.voxel-queue-empty {
  font-size: 7;
  text-color: #607B84;
  text-shadow: false;
}

.voxel-printing-output-socket {
  background: group(rect(#061014), border(-1, #4A8B91));
}

/* Shared components intentionally avoid card backgrounds. A section heading gets its
   weight from typography and a rule; the label text itself is authored uppercase. */
.sb-section-title {
  font-size: 6;
  text-color: #6F9099;
  text-shadow: false;
}

/* Material requirement rows: a flat data line, never a card. The 1px rule below each row is
   drawn by RequirementRow itself, since LDLib2 has no border-only rendering for a child. */
.sb-requirement-row {
  background: rect(#00000000);
}

.sb-requirement-name {
  font-size: 6;
  text-color: #8EADB5;
  text-shadow: false;
}

/* The item detail view. Hierarchy is carried by the large item sprite and the type scale around
   it; the region keeps its own background and nothing inside it gets a panel, a card or a frame. */
.sb-item-header {
  background: rect(#00000000);
}
.sb-item-header-text,
.sb-socket-cell {
  background: rect(#00000000);
}
.sb-item-name {
  font-size: 8;
  text-color: #E6F5F6;
  text-shadow: false;
}
.sb-item-count {
  font-size: 6;
  text-color: #7899A3;
  text-shadow: false;
}
/* Pure spacing elements: the band reserving room for the real output socket, the identity column
   beside the preview, and the two stack containers. None of them is visible. */
.sb-item-view-content,
.sb-craft-action-bar {
  background: rect(#00000000);
}

/* Materials: a flat table of rows. Only the empty-state hint needs a rule; each row draws its
   own separator. */
.sb-material-empty {
  font-size: 6;
  text-color: #607B84;
  text-shadow: false;
}

.sb-requirement-amount {
  font-size: 6;
  text-color: #CDE7E9;
  text-shadow: false;
}

.sb-requirement-row.sb-requirement-missing .sb-requirement-amount {
  text-color: #E2B66F;
}

.sb-requirement-row.sb-requirement-ok .sb-requirement-amount {
  text-color: #CDE7E9;
}

.sb-quantity-stepper {
  background: rect(#00000000);
}

/* The editable count has the same recessed treatment as search. */
.sb-quantity-value:host {
  background: rect(#081116);
  focus-overlay: border(-1, #63D8D4);
  padding-horizontal: 3;
}
.sb-quantity-value {
  font-size: 6;
  text-color: #D9EEEF;
  text-shadow: false;
}

.sb-stepper-button:host {
  base-background: rect(#1A2A35);
  hover-background: rect(#293F4B);
  pressed-background: rect(#101E27);
}

.sb-stepper-button .__button_text__ {
  font-size: 5;
  text-color: #C4D7DE;
  text-shadow: false;
}

.sb-stepper-button.__disabled__:host {
  base-background: rect(#111D25);
  hover-background: rect(#111D25);
  pressed-background: rect(#111D25);
}
.sb-stepper-button.__disabled__ .__button_text__ { text-color: #657C88; }

.sb-craft-button:host {
  base-background: rect(#20545D);
  hover-background: rect(#2C6B73);
  pressed-background: rect(#123E47);
}
.sb-craft-button .__button_text__ {
  font-size: 7;
  text-color: #E5FBFA;
  text-shadow: false;
}
.sb-craft-button.__disabled__:host {
  base-background: rect(#18262F);
  hover-background: rect(#18262F);
  pressed-background: rect(#18262F);
}
.sb-craft-button.__disabled__ .__button_text__ { text-color: #8095A0; }

/* The output frame: the one place the machine's item is shown. The preview layers are drawn by
   the component itself (it has to clip one over the other), so this only carries the frame. */
.sb-output-slot {
  background: group(rect(#081116), border(-1, #526A76));
}

/* The print-count line: the quantity control before a craft, the remaining count during one. */
.sb-print-count-bar {
  background: rect(#00000000);
}
.sb-print-count-remaining {
  font-size: 6;
  text-color: #9FCED2;
  text-shadow: false;
}

.voxel-queue-row { background: rect(#101D26); overflow-clip: true; }
.voxel-queue-row.voxel-queue-row-active { background: rect(#17343E); }
.voxel-queue-row.queue-recent { background: rect(#182D2C); }
.voxel-queue-name { font-size: 7; text-color: #CFE5E7; text-shadow: false; }
.voxel-queue-state { font-size: 6; text-color: #7897A1; text-shadow: false; }
.voxel-queue-row-active .voxel-queue-state { text-color: #8EDDD9; }
.voxel-queue-requester { font-size: 5; text-color: #617E87; text-shadow: false; }
.voxel-queue-progress .__progress-bar_bar-container__ { background: rect(#061015); }
.voxel-queue-progress .__progress-bar_bar__ { background: rect(#63C9C9); }

.voxel-queue-cancel:host,
.voxel-queue-toggle:host {
  base-background: rect(#00000000);
  hover-background: rect(#21343F);
  pressed-background: rect(#14262F);
}
.voxel-queue-cancel .__button_text__,
.voxel-queue-toggle .__button_text__ {
  font-size: 6;
  text-color: #9FBBC1;
  text-shadow: false;
}
.voxel-queue-cancel.__disabled__ .voxel-cancel-icon { opacity: 0.25; }

.machine-slot-socket {
  background: rect(#081116);
}
.player-slot-socket {
  background: rect(#0A141C);
}
.hotbar-slot-socket {
  background: rect(#0D1922);
}
